services:
  db:
    image: postgres:15-alpine
    container_name: prosefa-postgres
    restart: always
    env_file:
      - .properties
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: